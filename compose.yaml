services:
  bochupath:
    build: .
    restart: unless-stopped
    env_file: .env
    ports:
      - "127.0.0.1:3000:3000"
    volumes:
      - bochupath-data:/app/.bochupath
    security_opt:
      - no-new-privileges:true
volumes:
  bochupath-data:
